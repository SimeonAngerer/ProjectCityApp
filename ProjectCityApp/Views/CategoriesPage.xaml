<Page
    x:Class="ProjectCityApp.Views.CategoriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectCityApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <GridView ItemsSource="{Binding Categories}" Margin="5">
        <GridView.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Vertical" Tapped="StackPanel_Tapped" Tag="{Binding PK_CategoryID}">
                    <Image Height="200" Width="300" Source="{Binding Image}" Margin="2" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding Name}" Margin="2" />
                </StackPanel>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</Page>
