<Page
    x:Class="ProjectCityApp.Views.CategoriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectCityApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Categories, Mode=OneWay, Source={StaticResource Locator}}">

    <GridView ItemsSource="{Binding Categories}" Margin="5">
        <GridView.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Vertical">
                    <Image Height="200" Width="300" Source="https://dummyimage.com/150x100/000000/fff.jpg" Margin="2" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding Name}" Margin="2" />
                </StackPanel>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</Page>
